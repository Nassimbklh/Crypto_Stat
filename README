# Movie Lakehouse Local

Projet **Lakehouse local** avec **Airflow + PySpark + Spark (master/worker) + MinIO + Jenkins CI/CD**
(+ **Grafana/Loki/Promtail** pour les logs, **Azurite** pour simuler Azure Blob, **Metabase** en option).
Objectif : simuler un pipeline moderne compatible avec un futur déploiement sur **Azure (ADLS + Databricks)**.

---

## 🚀 Stack

- **Airflow** — Orchestration (TMDb ➜ Spark ➜ MinIO/Parquet)
- **Spark** — Transformations PySpark (Parquet)
- **MinIO (S3 compatible)** — Data Lake local (`s3a://lake/...`)
- **Azurite** — Émulateur **Azure Blob/ADLS** pour tester les chemins Azure
- **Jenkins** — CI/CD local (lint, build, smoke tests)
- **Grafana** — **Logs centralisés** & dashboards
- **Metabase (optionnel)** — Visualisation (Top 10, tendances)

---

## 📦 Installation

### 1) Cloner
```bash
git clone https://github.com/Nassimbklh/movie-lakehouse-local.git
cd movie-lakehouse-local